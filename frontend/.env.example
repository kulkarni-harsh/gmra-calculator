# LOCAL DEV: leave empty — Vite proxy handles /api/* → localhost:8000 (no CORS)
# CLOUD: set to your hosted backend URL, e.g. https://api.mrec.com
VITE_API_BASE_URL=

# Stripe publishable key (starts with pk_test_ or pk_live_)
VITE_STRIPE_PUBLISHABLE_KEY=
